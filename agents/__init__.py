"""ProcessIQ agents. Each agent reads/writes the shared AgentState (design Section 7).

Current implementations are contract-complete stubs (mock inference) so the full pipeline runs
end-to-end without GPUs. Owners replace internals with real models via the inference gateway.
"""
